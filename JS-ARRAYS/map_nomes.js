const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase());
//na arrow function é possível omitir a palavra-chave return, apagamos ela e podemos tirar as chaves de abertura e chave de fechamento da função

console.log(nomesPadronizados);
